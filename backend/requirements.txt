# Web server
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0
pydantic>=2.6

# Auth (email/password + JWT + OAuth)
bcrypt>=4.1
pyjwt>=2.8
httpx>=0.27

# Document parsing
pdfplumber>=0.11
pypdf>=5.0
python-docx>=1.1

# RAG stack (LangChain + local Ollama LLM / optional Grok + local HF embeddings + Chroma)
langchain-text-splitters>=0.2,<0.4
langchain-ollama>=0.2,<0.4
langchain-openai>=0.1.7,<0.3
langchain-anthropic>=0.2,<0.4
langchain-huggingface>=0.1,<0.3
langchain-chroma>=0.1.2,<0.3
langchain-community>=0.2,<0.4
chromadb>=0.5,<0.6
tiktoken>=0.7

# Local embedding model (no API key, runs on-device)
sentence-transformers>=3.0
